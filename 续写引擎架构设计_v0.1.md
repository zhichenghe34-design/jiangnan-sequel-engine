# 江南续写引擎 · 架构设计 v0.1

Date: 2026-06-08
状态：设计稿，未实现。

## 0. 这是什么

一个**长篇续写引擎**：用结构化的"故事记忆"保证长篇前后一致，用 `江南.skill` 保证笔法像江南，用大模型负责落笔。

它**不是**把原著切片存进向量库检索仿写的系统。向量库里存的是**二次创作的记忆与转述**，不是原文。这条线决定了整条链"删库即干净"，详见 §8。

## 1. 设计目标与非目标

目标：

- 续写长篇时不丢设定、不忘伏笔、人物不崩。
- 输出带江南感笔法（缺口人物、现实成本、段尾回疼等）。
- 全链路 quote-free：存的、传的、生成的都不含源作品原文句子。

非目标：

- 不追求"检索出原著某段"。
- 不做 fine-tune（把原文吸进权重，版权性质重且不可删，见 §9）。
- 不保证可商业出版；这是写作辅助，不是代笔保证。

## 2. 四层架构

```
┌─ ④ 校验层 ────────────────────────────────┐
│   evaluation 24 分门 + quote-free 扫描        │
├─ ③ 生成层 ────────────────────────────────┤
│   用户意图 → 检索①相关记忆 → 挂②skill → LLM 生成 │
├─ ② 风格层 ────────────────────────────────┤
│   江南.skill（core 协议 + preset + 评分门）     │
├─ ① 知识层（向量库 + 结构库）──────────────────┤
│   DS 蒸馏产出：笔法卡 / 设定卡 / 人物卡 /        │
│   伏笔台账 / 章节转述梗概（locator，不抄原文）    │
└──────────────────────────────────────────┘
```

- **①知识层**：续写引擎特有，本设计的主体。
- **②风格层**：复用已发布的 `江南.skill`，不重做。
- **③生成层**：把 ① 检索结果 + ② 协议拼成 prompt 喂 LLM。
- **④校验层**：复用 `江南.skill/core/evaluation.md` + 一个 quote-free 扫描器。

## 3. 知识层：五类卡片

所有卡片都是**转述/机制/定位**，禁止粘贴原文。每条带 `source_locator`（指向原著章节，便于人工回查），但正文字段只能是自己的话。

### 3.1 笔法机制卡 fingerprint_card
直接复用并迁移现有 `02_analysis` 成果（笔法地图、preset、跨系列机制层）。

| 字段 | 说明 |
|---|---|
| id | `FP-0001` |
| mechanism | 机制名，如"物/动作替代心理" |
| stage_config | 适用阶段/配置，如 P4 少年被卷入 |
| when_to_use | 触发条件（转述） |
| how | 操作要点（转述，不引原文） |
| source_locator | 证据位置（章/场景，不含原句） |

### 3.2 世界设定卡 setting_card

| 字段 | 说明 |
|---|---|
| id | `ST-0001` |
| name | 设定名（按项目决定**保留原专名**，见 §9） |
| kind | 组织/地点/力量体系/规则 |
| summary | 转述摘要 |
| constraints | 硬约束（什么不能违反） |
| source_locator | 出处 |

### 3.3 人物状态卡 character_card

| 字段 | 说明 |
|---|---|
| id | `CH-0001` |
| codename | 人物名（**保留原名**，优先续写代入感，见 §9） |
| gap | 人物缺口（江南感核心） |
| disguise | 伪装 |
| relations | 关系（指向其它 CH-id） |
| state_at | 状态快照，按章节版本化：`{chapter: 80, status: "..."}` |
| reality_cost | 当前他在为什么付代价（v1.1.1 成本层） |
| source_locator | 出处 |

### 3.4 伏笔台账 foreshadow_ledger

| 字段 | 说明 |
|---|---|
| id | `FS-0001` |
| planted_at | 埋设章节 |
| description | 伏笔内容（转述） |
| status | open / paid / abandoned |
| paid_at | 回收章节（若已回收） |
| note | 续写时如何圆 |

### 3.5 章节转述梗概 chapter_synopsis

| 字段 | 说明 |
|---|---|
| id | `SY-080` |
| chapter | 章号 |
| synopsis | **自己的话**复述本章剧情，120–300 字 |
| key_objects | 本章承重的小物/动作 |
| emotional_turn | 情绪转折点 |
| source_locator | 原著章节定位 |

## 4. 切块与向量化

- **切块单位 = 一张卡片**（而非定长字符窗）。卡片本身已是语义完整单元，天然适合检索。
- **embedding**：中文优先 `bge-large-zh-v1.5` 或 `Qwen3-embedding`；本地可跑。
- **入库内容**：embedding 用 `summary/synopsis/description` 字段；`source_locator` 等元数据存 metadata，不参与向量。
- 卡片同时落一份**结构化 JSON/SQLite**（便于按章号、status 精确查询，如"取第 80 章前所有 open 伏笔"）。向量库管语义召回，结构库管精确过滤，两者并用。

## 5. 向量库选型

| 方案 | 适用 | 备注 |
|---|---|---|
| **Chroma** | MVP 首选 | 本地、轻、Python 友好 |
| LanceDB | 数据量大/要列式 | 嵌入式、性能好 |
| FAISS | 纯向量、要极致性能 | 自己管元数据，较裸 |

MVP 用 Chroma（向量）+ SQLite（结构）即可，全本地，符合"删库即干净"。

## 6. 生成层：一次续写的数据流

```
用户输入：续写到第 N 章，想要的走向/情绪/preset
   │
   ├─ 结构库精确召回：第 N 章前所有 open 伏笔、相关人物最新 state_at、涉及设定卡
   ├─ 向量库语义召回：与"本段意图"最相关的笔法卡 + 邻近章节梗概
   ├─ rerank：按章节距离 + 相关度重排，控制注入预算
   │
   ▼
组装 prompt：
   [系统] 江南.skill core 协议 + 目标 preset（来自②）
   [记忆] 召回的设定/人物/伏笔/梗概（来自①，全转述）
   [任务] 用户意图 + 内部卡（缺口/现实成本/段尾）
   │
   ▼
LLM 生成续写正文
   │
   ▼
④校验：evaluation 24 分门自检 + quote-free 扫描
   │
   ├─ 通过 → 产出；并回写知识层（新章节梗概、伏笔状态、人物 state_at 更新）
   └─ 不过 → 按失败模式定向修订（优先补缺口/现实成本/遮蔽）
```

关键：**生成后要回写知识层**——续写产生的新剧情，反过来成为下一章的记忆。这是引擎能滚动长篇的核心。

## 7. 与 江南.skill 的对接

- skill 不改、不复制进本工程，**以路径/子模块方式引用**：生成层在组装 prompt 时读 `江南.skill/core/protocol.md`、`presets.md`，校验层读 `core/evaluation.md`。
- 升级解耦：skill 升版本（如 v1.2）只要接口字段不变，引擎无需改。
- 二者关系：`江南.skill` = 怎么写得像江南；本引擎 = 写长了不忘事、不崩设定。

## 8. DS 蒸馏：批处理提示词模板

DS 负责把原著读成上面五类卡片。**唯一红线：输出只能是转述/机制/定位，禁止粘贴原文句子。**

```text
你是笔法与剧情分析器，不是摘录器。
读入：原著某章/某段（仅你本地阅读，不写回输出）。
输出：结构化卡片 JSON，分五类（fingerprint/setting/character/foreshadow/synopsis）。

硬规则：
1. 所有文本字段必须是你自己的话（转述/概括/机制描述）。
2. 严禁出现原著连续原句、整段描写；人物/地点专名按项目决定**保留原名**（提升代入感），但不得因此带出整句原文。
3. 每张卡带 source_locator（章节/场景定位），但不得在正文字段抄原文。
4. synopsis 120–300 字，复述剧情，不复制描写。
5. 拿不准是否算原句时，改写得更概括。

输出格式：见架构文档 §3 各卡字段。
```

聚焦建议：续写龙族正传，先蒸馏**龙族系列的 setting/character/foreshadow/synopsis**，笔法卡直接用现有 `02_analysis` 跨系列成果，不必重啃全集 1175 文件。

## 9. quote-free 红线（贯穿全链）

| 环节 | 约束 |
|---|---|
| DS 蒸馏 | 只输出转述/机制/定位，§8 规则 |
| 入库 | 入库前过一遍 quote-free 扫描，命中疑似原句的卡片打回重写 |
| 生成 | prompt 注入的都是转述记忆，不注入原文 |
| 校验 | 输出再扫一次，防止模型自己复现源近句式 |
| 分发 | 公开仓库永不含原著原文；原文只在本地阅读态存在 |

为什么不 fine-tune：把原文做训练集 = 原文被吸进权重，性质比"存原文"更重且**删不掉**，与"删库即干净"的兜底直接冲突。坚持 RAG + 转述知识。

**专名边界（项目决定 2026-06-08）**：本工程**保留**源作品人物/地点专名以提升续写代入感。因此这里的 quote-free 指**不复制原文句子、段落、描写**，而非"不出现专名"。这把作品性质从"纯原创演绎"推向**"使用专名的非盈利同人"，版权风险相应上升**；兜底仍是：非商业 + 不公开散播原著原文 + 必要时删库。专名出现在生成正文与知识卡，但**原文句子始终不入库、不分发**。

## 10. MVP 分阶段

- **M0 脚手架**：定卡片 schema（JSON）、建 Chroma + SQLite、写 quote-free 扫描器。
- **M1 知识灌注**：DS 蒸馏龙族正传 → 设定/人物/伏笔/梗概卡入库；笔法卡迁移自 `02_analysis`。
- **M2 单章续写**：实现 §6 数据流，跑通"给定第 N 章意图 → 检索 → 挂 skill → 生成 → 24 分门校验"。
- **M3 回写闭环**：生成后自动更新伏笔状态、人物 state_at、新增章节梗概。
- **M4 体验**：简单 CLI/界面，让用户只填"续写意图 + preset"。

## 11. 待定问题

- ~~代号映射：保留可识别人名还是全换原创名？~~ **[已决 2026-06-08] 保留原名**，优先续写代入感；代价是版权性质更靠近同人，兜底见 §9。
- 知识层是否纳入版本控制：建议结构库（卡片 JSON）可版本化，但**绝不与含原文的任何东西同库**。
- 评分门是否要为"长篇连贯性"新增评分项（伏笔回收率、人物状态一致性）。
